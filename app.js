let counter = prompt("Dörd rəqəmli ədəd daxil edin!");
if (counter.length === 4) {
    let a = Number(num[0]) + Number(num[1]) + Number(num[2]) + Number(num[3]);
    console.log(a);
} else {
    console.log("Dörd rəqəmli bir ədəd daxil edin!");
}